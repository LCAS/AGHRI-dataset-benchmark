device: 0
seed: 42
benchmark_fixed_input: true
benchmark_warmup: 20
benchmark_iters: 100
summary_primary_mode: bev
summary_primary_iou: 0.50
prefer_best_checkpoint: true
save_best_metric: aghri/bev_ap_mean
save_best_rule: greater
max_keep_ckpts: 3
project: /work/users/pwariyapperuma/aghri_second_finetune/outputs
name: second_aghri_seed42
models:
  - name: second_aghri_finetuned_seed42
    config: /work/users/pwariyapperuma/aghri_second_finetune/configs/second_aghri_finetune_seed42.py
    train: true
    checkpoint: /work/users/pwariyapperuma/aghri_second_finetune/checkpoints/generic/second_hv_secfpn_8xb6-80e_kitti-3d-3class-b086d0a3.pth
    work_dir: /work/users/pwariyapperuma/aghri_second_finetune/outputs/second_aghri_seed42/full
notes: >
  AGHRI train/val-only SECOND fine-tuning run initialized from the public
  KITTI-pretrained SECOND checkpoint. Held-out AGHRI test data is intentionally
  excluded from the staged training root.
